% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/package.R, R/rasterPoints.R
\docType{package}
\name{rasterPoints}
\alias{rasterPoints}
\title{Plot scatter points using rasterImage.}
\usage{
rasterPoints(
  x,
  col = "black",
  cex = 1,
  interpolate = F,
  ncores = 0,
  colorder = "default",
  ...
)
}
\arguments{
\item{x}{Matrix of data to plot. Must have 2 columns.}

\item{col}{Character vector with lenght = nrow(x) or lenght = 1 (same color for all points).}

\item{cex}{Integer. See par("cex").}

\item{interpolate}{Logical. Passed to rasterImage.}

\item{ncores}{Integer. Number of cores used (OpenMP). Defaul = 0 (max avaiable cores).}

\item{colorder}{Character vector with the color priority order. Colors with higher colorder index has priority over the others. The "default" behavior is set the point color only the first time and ignore any new attempt to set the point color.}

\item{...}{Graphic parameters passed to rasterImage/plot (see par()).}
}
\value{
Nothing.
}
\description{
Plot scatter points using rasterImage. Open a graphic device if grDevices::dev.cur() == 1 (null device).
}
\examples{
data<-matrix(rnorm(10^6), ncol = 2)
system.time({
  plot(data, type="n")
  points(data, pch = ".", type = "p", cex=1, col = "blue")
})
system.time({
  plot(data, type="n")
  rasterPoints(data, cex=1, col = "blue")
})
}
\author{
Sérgio Fortier
}
\keyword{plot,}
\keyword{points.}
\keyword{raster,}
\keyword{scatter,}
